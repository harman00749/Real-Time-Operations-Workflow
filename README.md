# Real-Time-Operations-Workflow
Real-time verification workflow portal built with React and WebSockets. Includes Kanban task lanes, approve/reject status updates, reconnect handling, offline-safe actions, localStorage persistence, loading states, analytics logging, and a clean enterprise dashboard UI.
